docker run -d \
  --name=[NAME] \
  -e PUID=1000 \
  -e PGID=1000 \
  -e TZ=Etc/UTC \
  -e PASSWORD=password [PASSWORD] \
  -p [PORT]:8443 \
  -v ~/.config/code-server/config.yaml:/config \
  lscr.io/linuxserver/code-server:latest